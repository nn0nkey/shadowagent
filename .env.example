# ShadowAgent 环境配置示例
# 复制此文件为 .env 并填入你的配置

# ============================================================
# LLM 配置（魔塔社区 API）
# ============================================================

# 主攻手 Agent 配置
LLM_PROVIDER=openai
LLM_MODEL=deepseek-ai/DeepSeek-V3.2

# 顾问 Agent 配置（可选，默认使用 LLM_MODEL）
ADVISOR_LLM_PROVIDER=openai
ADVISOR_LLM_MODEL=deepseek-ai/DeepSeek-V3.2

# Reflector Agent 配置（可选，默认使用 LLM_MODEL）
REFLECTOR_LLM_MODEL=deepseek-ai/DeepSeek-V3.2

# 魔塔社区 API 配置
OPENAI_API_BASE=https://api-inference.modelscope.cn/v1/
OPENAI_API_KEY=your-modelscope-api-key-here

# ============================================================
# 可选：其他 LLM 提供商配置
# ============================================================

# DeepSeek 官方 API（如果使用）
# DEEPSEEK_API_KEY=your-deepseek-api-key
# DEEPSEEK_API_BASE=https://api.deepseek.com

# OpenAI 官方 API（如果使用）
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_API_BASE=https://api.openai.com/v1

# 通义千问（如果使用）
# DASHSCOPE_API_KEY=your-dashscope-api-key

# ============================================================
# Agent 行为配置
# ============================================================

# 顾问介入阈值（连续失败多少次后咨询顾问）
ADVISOR_FAILURE_THRESHOLD=3

# 顾问咨询间隔（每隔多少次尝试咨询一次）
ADVISOR_CONSULTATION_INTERVAL=5

# 启用智能路由（检测重复模式、失败模式等）
ENABLE_SMART_ROUTING=true

# 启用元认知评估（信心评估）
ENABLE_METACOGNITION=true

# 使用 LLM 进行元认知评估（更准确但更慢）
USE_LLM_METACOGNITION=false

# ============================================================
# 知识库配置
# ============================================================

# 启用知识库
ENABLE_KNOWLEDGE_BASE=true

# 知识库路径
KNOWLEDGE_BASE_PATH=knowledge_base/

# ============================================================
# Docker 配置
# ============================================================

# Docker 镜像名称
DOCKER_IMAGE=shadowagent-kali:latest

# Docker 容器名称前缀
DOCKER_CONTAINER_PREFIX=shadowagent

# ============================================================
# 日志配置
# ============================================================

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 启用详细日志
VERBOSE=false

# ============================================================
# 模型配置说明
# ============================================================

# 不同 Agent 可以使用不同的模型：
# 
# 1. 主攻手 (Attacker)：
#    - 使用 LLM_MODEL
#    - 负责执行具体的攻击操作
#    - 建议：使用强大的模型（如 DeepSeek-V3.2）
#
# 2. 顾问 (Advisor)：
#    - 使用 ADVISOR_LLM_MODEL（可选）
#    - 负责战略分析和建议
#    - 建议：使用强大的模型（如 DeepSeek-V3.2）
#
# 3. 反思器 (Reflector)：⭐ 新增
#    - 使用 REFLECTOR_LLM_MODEL（可选）
#    - 负责审核结果真实性、失败分级
#    - 建议：使用强大的模型（如 DeepSeek-V3.2）
#
# 如果不单独配置，所有 Agent 都使用 LLM_MODEL

# ============================================================
# 魔塔社区可用模型
# ============================================================

# DeepSeek 系列（推荐）
# - deepseek-ai/DeepSeek-V3.2 (最强，推荐)
# - deepseek-ai/DeepSeek-V2.5
# - deepseek-ai/deepseek-coder-33b-instruct

# Qwen 系列
# - Qwen/Qwen2.5-Coder-32B-Instruct
# - Qwen/Qwen2.5-72B-Instruct
# - qwen/Qwen2.5-7B-Instruct

# 其他模型
# - 01-ai/Yi-1.5-34B-Chat
# - internlm/internlm2_5-20b-chat
